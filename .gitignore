__pycache__/
*.pyc
.venv/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Working artifacts that may hold user document content — never commit
*.pnmap
*.pen.*
*.restored.*

# Fetched GLiNER model (provisioned at build time, never committed)
gliner_model/

# Internal agent build-instructions — kept local, not published
CLAUDE.md
